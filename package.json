{
	"name": "without-keys",
	"version": "0.0.3",
	"description": "Returns a new object without the keys passed as second parameter, and without modifying the first object",
	"repository": {
		"type": "git",
		"url": "git@github.com:srcagency/without-keys.git"
	},
	"scripts": {
		"test": "node test.js"
	},
	"author": {
		"name": "René T. Nielsen",
		"email": "rene@src.agency"
	},
  	"license": "MIT",
  	"devDependencies": {
  		"tape": "*"
  	}
}
